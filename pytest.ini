[pytest]
addopts = -ra --strict-markers --alluredir=allure-results
markers =
    smoke: Critical smoke suite
    regression: Full regression suite
    optional_skip: Tests that can be skipped with --skip-optional
    ui: UI automation test
    api: API automation test
    known_issue: Known issue marker
testpaths = tests
